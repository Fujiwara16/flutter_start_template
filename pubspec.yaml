name: surat_saheli_mobile_client
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.1.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  bloc: ^8.1.1
  injectable_generator: ^2.1.4
  flutter_bloc: ^8.1.2
  injectable: ^2.1.0
  get_it: ^7.2.0
  build_runner: ^2.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
